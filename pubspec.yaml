name: keyboard_visibility_pro
description: A new Flutter keyboard visibility package.
version: 1.0.0
homepage: https://github.com/sanjidbillah/keyboard_visibility_pro

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: